class Score {
	leftPlayer: number;
	rightPlayer: number;

	constructor(leftPlayer: number, rightPlayer: number) {
		this.leftPlayer = leftPlayer;
		this.rightPlayer = rightPlayer;
	}
}

export default Score;